---
phase: 01-foundation-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - next.config.ts
  - eslint.config.mjs
  - package.json
  - src/utils/style.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Developer can run pnpm dev and see a page with dark Obsidian background (#0e0e10)"
    - "Inter font loads for body text, JetBrains Mono loads for code/mono text"
    - "Tailwind utility classes like bg-obsidian-surface-well produce correct colors"
    - "Surface hierarchy colors are available as Tailwind classes"
    - "ESLint max-lines rule rejects files over 300 lines"
    - "Unused AI provider packages are removed from package.json"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS custom properties for Obsidian Deep + shadcn variable overrides"
      contains: "--background: #0e0e10"
    - path: "tailwind.config.ts"
      provides: "Tailwind extend with obsidian color tokens and custom radius"
      contains: "obsidian:"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter + JetBrains Mono fonts, dark body class"
      contains: "Inter"
    - path: "src/utils/style.ts"
      provides: "cn() utility for class merging"
      contains: "twMerge"
    - path: "eslint.config.mjs"
      provides: "300-line max enforcement"
      contains: "max-lines"
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variable references in Tailwind theme extend"
      pattern: "hsl\\(var\\(--"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "imports globals.css stylesheet"
      pattern: "import.*globals"
---

<objective>
Set up the Obsidian Deep design foundation: CSS custom properties, Tailwind configuration, fonts, directory scaffolding, ESLint rules, and package cleanup.

Purpose: This is the base layer that all subsequent plans and phases build upon. Without correct design tokens in CSS and Tailwind, no component will render correctly.
Output: A running dev server showing the Obsidian Deep dark theme with correct fonts, plus a clean dependency tree and enforced code size limits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
@design/DESIGN.md

<interfaces>
<!-- Existing files the executor must work with -->

From src/app/globals.css:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  margin: 0;
  padding: 0;
}
```

From src/app/layout.tsx:
```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Deep Research",
  description: "AI-powered deep research tool",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

From tailwind.config.ts:
```typescript
// Has shadcn default theme with hsl(var(--xxx)) references
// Has tailwindcssAnimate and tailwindcssTypography plugins
// Has accordion keyframes
// borderRadius: lg=var(--radius), md=calc(var(--radius)-2px), sm=calc(var(--radius)-4px)
```

From components.json:
```json
{
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/utils/style",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Obsidian Deep design tokens and configure Tailwind</name>
  <files>src/app/globals.css, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/utils/style.ts</files>
  <read_first>
    src/app/globals.css
    tailwind.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    .planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
    design/DESIGN.md
    components.json
  </read_first>
  <action>
    **1. Create `src/utils/style.ts`** — the `cn()` utility:
    ```typescript
    import { type ClassValue, clsx } from "clsx";
    import { twMerge } from "tailwind-merge";

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```
    This is required by shadcn/ui components (components.json aliases utils to `@/utils/style`).

    **2. Replace `src/app/globals.css`** entirely with:

    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      :root {
        /* Obsidian Deep — dark only, no light theme toggle */
        --background: #0e0e10;
        --foreground: #e5e1e4;

        --card: #201f22;
        --card-foreground: #e5e1e4;

        --popover: #353437;
        --popover-foreground: #e5e1e4;

        --primary: #c0c1ff;
        --primary-foreground: #1000a9;

        --secondary: #2a2a2c;
        --secondary-foreground: #e5e1e4;

        --muted: #1c1b1d;
        --muted-foreground: #c7c4d7;

        --accent: #2a2a2c;
        --accent-foreground: #e5e1e4;

        --destructive: #ffb4ab;
        --destructive-foreground: #93000a;

        --border: rgba(70, 69, 84, 0.15);
        --input: rgba(70, 69, 84, 0.15);
        --ring: #c0c1ff;

        --radius: 0.75rem;
      }

      * {
        @apply border-[var(--border)];
      }

      body {
        @apply bg-background text-foreground;
        font-feature-settings: "rlig" 1, "calt" 1;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }
    }
    ```
    NOTE: All shadcn CSS variables use direct hex values (not `hsl()`) because the Obsidian Deep design system specifies colors as hex. The shadcn default uses `hsl(var(--xxx))` in Tailwind — we will change the Tailwind config to use raw `var(--xxx)` instead (see step 3).

    **3. Replace `tailwind.config.ts`** with the Obsidian Deep configuration:

    ```typescript
    import type { Config } from "tailwindcss";
    import tailwindcssAnimate from "tailwindcss-animate";
    import tailwindcssTypography from "@tailwindcss/typography";

    export default {
      darkMode: ["class"],
      content: [
        "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
      ],
      theme: {
        extend: {
          colors: {
            /* Obsidian Deep semantic tokens */
            obsidian: {
              surface: "#131315",
              "surface-well": "#0e0e10",
              "surface-deck": "#1c1b1d",
              "surface-sheet": "#201f22",
              "surface-raised": "#2a2a2c",
              "surface-float": "#353437",
              "surface-bright": "#39393b",
              primary: "#c0c1ff",
              "primary-deep": "#8083ff",
              "primary-seed": "#6366f1",
              secondary: "#adc6ff",
              tertiary: "#d0bcff",
              "on-surface": "#e5e1e4",
              "on-surface-var": "#c7c4d7",
              outline: "#908fa0",
              "outline-ghost": "rgba(70, 69, 84, 0.15)",
              error: "#ffb4ab",
              "error-bg": "#93000a",
            },
            /* shadcn/ui CSS variable references (raw values, not hsl-wrapped) */
            background: "var(--background)",
            foreground: "var(--foreground)",
            card: {
              DEFAULT: "var(--card)",
              foreground: "var(--card-foreground)",
            },
            popover: {
              DEFAULT: "var(--popover)",
              foreground: "var(--popover-foreground)",
            },
            primary: {
              DEFAULT: "var(--primary)",
              foreground: "var(--primary-foreground)",
            },
            secondary: {
              DEFAULT: "var(--secondary)",
              foreground: "var(--secondary-foreground)",
            },
            muted: {
              DEFAULT: "var(--muted)",
              foreground: "var(--muted-foreground)",
            },
            accent: {
              DEFAULT: "var(--accent)",
              foreground: "var(--accent-foreground)",
            },
            destructive: {
              DEFAULT: "var(--destructive)",
              foreground: "var(--destructive-foreground)",
            },
            border: "var(--border)",
            input: "var(--input)",
            ring: "var(--ring)",
          },
          borderRadius: {
            lg: "var(--radius)",
            md: "calc(var(--radius) - 2px)",
            sm: "calc(var(--radius) - 4px)",
          },
          fontFamily: {
            sans: ["var(--font-inter)", "system-ui", "sans-serif"],
            mono: ["var(--font-jetbrains-mono)", "monospace"],
          },
          keyframes: {
            "accordion-down": {
              from: { height: "0" },
              to: { height: "var(--radix-accordion-content-height)" },
            },
            "accordion-up": {
              from: { height: "var(--radix-accordion-content-height)" },
              to: { height: "0" },
            },
          },
          animation: {
            "accordion-down": "accordion-down 0.2s ease-out",
            "accordion-up": "accordion-up 0.2s ease-out",
          },
        },
      },
      plugins: [tailwindcssAnimate, tailwindcssTypography],
    } satisfies Config;
    ```
    KEY CHANGE: Replaced `hsl(var(--xxx))` with `var(--xxx)` for all shadcn color references. Since our CSS variables contain full hex values (not HSL channel values), the hsl() wrapper is wrong and would break colors.

    **4. Replace `src/app/layout.tsx`** with font-loaded layout:
    ```typescript
    import type { Metadata } from "next";
    import { Inter, JetBrains_Mono } from "next/font/google";
    import "./globals.css";

    const inter = Inter({
      subsets: ["latin"],
      variable: "--font-inter",
      weight: ["400", "600"],
      display: "swap",
    });

    const jetbrainsMono = JetBrains_Mono({
      subsets: ["latin"],
      variable: "--font-jetbrains-mono",
      weight: ["400", "500"],
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "Deep Research",
      description: "AI-powered deep research tool",
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en" className="dark">
          <body
            className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased bg-background text-foreground`}
          >
            {children}
          </body>
        </html>
      );
    }
    ```
    KEY: `className="dark"` on `<html>` enables dark mode. Font CSS variables (`--font-inter`, `--font-jetbrains-mono`) are set by next/font and referenced in Tailwind config fontFamily.

    **5. Update `src/app/page.tsx`** to show Obsidian Deep themed placeholder:
    ```typescript
    export default function Home() {
      return (
        <main className="flex min-h-screen items-center justify-center bg-obsidian-surface-well">
          <div className="text-center">
            <h1 className="text-4xl font-semibold tracking-tight text-obsidian-on-surface">
              Deep Research
            </h1>
            <p className="mt-4 text-sm leading-relaxed text-obsidian-on-surface-var font-mono">
              v1.0 rewrite in progress
            </p>
          </div>
        </main>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - globals.css contains all Obsidian Deep CSS custom properties (--background: #0e0e10, --foreground: #e5e1e4, --primary: #c0c1ff, etc.)
    - tailwind.config.ts has obsidian color tokens and uses var(--xxx) not hsl(var(--xxx)) for shadcn variables
    - layout.tsx loads Inter and JetBrains Mono via next/font with CSS variable strategy
    - src/utils/style.ts exports cn() function using clsx + tailwind-merge
    - pnpm build succeeds without errors
    - page.tsx uses Obsidian Deep Tailwind classes (bg-obsidian-surface-well, text-obsidian-on-surface)
  </done>
  <acceptance_criteria>
    - grep -c "obsidian:" tailwind.config.ts returns >= 1
    - grep "\\-\\-background: #0e0e10" src/app/globals.css returns match
    - grep "\\-\\-primary: #c0c1ff" src/app/globals.css returns match
    - grep "var(--font-inter)" tailwind.config.ts returns match
    - grep "var(--font-jetbrains-mono)" tailwind.config.ts returns match
    - grep "cn(" src/utils/style.ts returns match
    - grep "className=\"dark\"" src/app/layout.tsx returns match
    - grep "bg-obsidian-surface-well" src/app/page.tsx returns match
    - pnpm build exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add ESLint max-lines rule and create directory scaffolding</name>
  <files>eslint.config.mjs, package.json, next.config.ts</files>
  <read_first>
    eslint.config.mjs
    package.json
    next.config.ts
  </read_first>
  <action>
    **1. Update `eslint.config.mjs`** to add the 300-line max-files rule per CONTEXT.md decision:

    ```javascript
    import { dirname } from "path";
    import { fileURLToPath } from "url";
    import { FlatCompat } from "@eslint/eslintrc";

    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);

    const compat = new FlatCompat({
      baseDirectory: __dirname,
    });

    const eslintConfig = [
      ...compat.extends("next/core-web-vitals", "next/typescript"),
      {
        rules: {
          "@typescript-eslint/no-explicit-any": "off",
          "max-lines": [
            "error",
            {
              max: 300,
              skipBlankLines: true,
              skipComments: true,
            },
          ],
        },
      },
    ];

    export default eslintConfig;
    ```

    **2. Create directory scaffolding** with empty index/placeholder files so imports resolve:

    ```bash
    mkdir -p src/components/ui
    mkdir -p src/hooks
    mkdir -p src/store
    mkdir -p src/utils
    mkdir -p src/lib
    mkdir -p src/types
    ```

    Create a `src/types/index.ts` that re-exports from types.d.ts or provides base types:
    ```typescript
    // Base types for Deep Research v1.0
    // Extended during implementation phases
    ```

    **3. Remove unused AI provider packages** from package.json. These are the packages to remove (keeping only `@ai-sdk/google` and `@ai-sdk/openai` as per CONTEXT.md):
    - `@ai-sdk/anthropic`
    - `@ai-sdk/azure`
    - `@ai-sdk/deepseek`
    - `@ai-sdk/google-vertex`
    - `@ai-sdk/mistral`
    - `@ai-sdk/openai-compatible`
    - `@ai-sdk/xai`
    - `@openrouter/ai-sdk-provider`
    - `ollama-ai-provider`

    Also remove these that are not needed for v1.0 foundation:
    - `next-themes` (dark-only, no theme toggle)
    - `react-use-pwa-install` (Phase 9)
    - `react-zoom-pan-pinch` (Phase 5)
    - `katex`, `rehype-katex`, `remark-math` (Phase 5)
    - `mermaid` (Phase 5)
    - `@xiangfa/mdeditor` (Phase 5)
    - `pdfjs-dist` (Phase 7)
    - `@zip.js/zip.js` (Phase 7)
    - `react-markdown`, `rehype-highlight`, `rehype-raw`, `remark-breaks`, `remark-gfm`, `unist-util-visit` (Phase 5)
    - `@types/hast` (devDep, Phase 5)
    - `@types/jsdom` (devDep, not needed)
    - `copy-to-clipboard`, `file-saver`, `fuse.js`, `react-hook-form`, `react-resizable-panels` (later phases)
    - `@serwist/next`, `serwist` (Phase 9)
    - `i18next`, `i18next-browser-languagedetector`, `i18next-resources-to-backend`, `react-i18next` (Phase 9)
    - `@hookform/resolvers` (Phase 6)

    Run `pnpm install` after editing package.json to update the lockfile.

    **4. Clean up `next.config.ts`** — remove the `transpilePackages` for `pdfjs-dist` and `mermaid` since those packages are removed:
    ```typescript
    import type { NextConfig } from "next";
    import pkg from "./package.json";

    const BUILD_MODE = process.env.NEXT_PUBLIC_BUILD_MODE;

    const nextConfig: NextConfig = {
      experimental: {
        reactCompiler: true,
      },
      env: {
        NEXT_PUBLIC_VERSION: pkg.version,
      },
    };

    if (BUILD_MODE === "export") {
      nextConfig.output = "export";
      nextConfig.webpack = (config) => {
        config.module.rules.push({
          test: /src\/app\/api/,
          loader: "ignore-loader",
        });
        config.module.rules.push({
          test: /src\/middleware/,
          loader: "ignore-loader",
        });
        return config;
      };
    } else if (BUILD_MODE === "standalone") {
      nextConfig.output = "standalone";
    }

    export default nextConfig;
    ```

    **5. Remove `@serwist/next/typings` from tsconfig.json** types array (serwist removed):
    Change `"types": ["./src/types", "@serwist/next/typings"]` to `"types": ["./src/types"]`.
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && pnpm lint 2>&1 | tail -5 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ESLint config has max-lines: 300 with skipBlankLines and skipComments
    - Directory scaffolding exists: src/components/ui, src/hooks, src/store, src/utils, src/lib, src/types
    - Unused AI provider packages removed from package.json
    - Only @ai-sdk/google and @ai-sdk/openai remain as AI SDK packages
    - pnpm install succeeds with clean lockfile
    - pnpm build succeeds
    - pnpm lint succeeds
  </done>
  <acceptance_criteria>
    - grep "max-lines" eslint.config.mjs returns match
    - grep "300" eslint.config.mjs returns match
    - grep "@ai-sdk/anthropic" package.json returns NO match
    - grep "@ai-sdk/azure" package.json returns NO match
    - grep "@ai-sdk/deepseek" package.json returns NO match
    - grep "@ai-sdk/google-vertex" package.json returns NO match
    - grep "@ai-sdk/mistral" package.json returns NO match
    - grep "@ai-sdk/openai-compatible" package.json returns NO match
    - grep "@ai-sdk/xai" package.json returns NO match
    - grep "@openrouter/ai-sdk-provider" package.json returns NO match
    - grep "ollama-ai-provider" package.json returns NO match
    - grep "@ai-sdk/google" package.json returns match (kept)
    - grep "@ai-sdk/openai" package.json returns match (kept)
    - test -d src/components/ui returns 0
    - test -d src/hooks returns 0
    - test -d src/store returns 0
    - test -d src/utils returns 0
    - test -d src/lib returns 0
    - pnpm build exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors and http://localhost:3000 shows dark background with Inter font
2. `grep "obsidian:" tailwind.config.ts` confirms Obsidian color tokens present
3. `grep "max-lines" eslint.config.mjs` confirms 300-line rule active
4. `grep "@ai-sdk/anthropic" package.json` returns no match (packages removed)
5. `pnpm build` completes successfully
6. `pnpm lint` completes without errors
</verification>

<success_criteria>
- Obsidian Deep CSS custom properties defined in globals.css matching UI-SPEC values exactly
- Tailwind config extends with obsidian color tokens and uses var() (not hsl(var())) for shadcn variables
- Inter + JetBrains Mono loaded via next/font with CSS variables
- Root layout applies dark class and font variables to body
- cn() utility available at src/utils/style.ts
- ESLint enforces max 300 lines per file
- All unused AI provider packages removed, only @ai-sdk/google and @ai-sdk/openai remain
- Directory scaffolding in place: components/ui, hooks, store, utils, lib, types
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md`
</output>
