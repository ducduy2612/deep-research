---
phase: 01-foundation-and-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/form.tsx
  - src/components/ui/label.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/resizable.tsx
  - src/lib/errors.ts
  - src/lib/env.ts
  - src/lib/logger.ts
  - src/lib/storage.ts
  - src/types/index.ts
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-05

must_haves:
  truths:
    - "All 16 shadcn/ui primitives are installed and render with Obsidian Deep colors"
    - "Floating elements (dialog, popover, dropdown-menu) show glassmorphism effect"
    - "AppError class provides structured error format with code, message, and context"
    - "env.ts validates environment variables with Zod at startup, failing fast on invalid config"
    - "logger outputs JSON in production and pretty-print in development"
    - "Storage wrapper provides Zod-validated get/set with localforage"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "shadcn button component"
      contains: "buttonVariants"
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn dialog component"
      contains: "Dialog"
    - path: "src/lib/errors.ts"
      provides: "AppError class with error codes"
      contains: "class AppError"
    - path: "src/lib/env.ts"
      provides: "Zod-validated env configuration"
      contains: "envSchema"
    - path: "src/lib/logger.ts"
      provides: "Structured logger"
      contains: "logger"
    - path: "src/lib/storage.ts"
      provides: "Zod-validated storage wrapper"
      contains: "localforage"
  key_links:
    - from: "src/lib/storage.ts"
      to: "localforage"
      via: "localforage import for IndexedDB persistence"
      pattern: "import.*localforage"
    - from: "src/lib/env.ts"
      to: "zod"
      via: "Zod schema validation"
      pattern: "z\\.(object|string|enum)"
---

<objective>
Install all shadcn/ui primitives with Obsidian Deep theming and build the four infrastructure utilities (AppError, env, logger, storage).

Purpose: These components and utilities form the reusable foundation for every subsequent phase. Installing them now with correct Obsidian Deep styling means all future work uses the same tested primitives.
Output: 16 shadcn/ui component files in src/components/ui/ and 4 infrastructure modules in src/lib/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-UI-SPEC.md

<!-- Depends on Plan 01 output -->
@.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — globals.css CSS variables the shadcn components consume -->
From src/app/globals.css:
```
--background: #0e0e10
--foreground: #e5e1e4
--card: #201f22
--popover: #353437
--primary: #c0c1ff
--primary-foreground: #1000a9
--secondary: #2a2a2c
--muted: #1c1b1d
--muted-foreground: #c7c4d7
--accent: #2a2a2c
--destructive: #ffb4ab
--border: rgba(70, 69, 84, 0.15)
--input: rgba(70, 69, 84, 0.15)
--ring: #c0c1ff
--radius: 0.75rem
```

From src/utils/style.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install all shadcn/ui primitives</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/select.tsx, src/components/ui/tabs.tsx, src/components/ui/accordion.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/form.tsx, src/components/ui/label.tsx, src/components/ui/popover.tsx, src/components/ui/scroll-area.tsx, src/components/ui/separator.tsx, src/components/ui/slider.tsx, src/components/ui/textarea.tsx, src/components/ui/tooltip.tsx, src/components/ui/resizable.tsx</files>
  <read_first>
    components.json
    tailwind.config.ts
    src/app/globals.css
    src/utils/style.ts
    .planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
  </read_first>
  <action>
    **1. Install all 16 shadcn/ui components** using the CLI. Run from project root:

    ```bash
    pnpm dlx shadcn@latest add button card dialog input select tabs accordion dropdown-menu form label popover scroll-area separator slider textarea tooltip resizable --yes
    ```

    This will generate all components into `src/components/ui/` using the new-york style preset defined in components.json. The components will automatically use the CSS variables from globals.css (which Plan 01 set to Obsidian Deep values).

    **2. After installation, verify** each component file exists and contains proper imports from `@/utils/style` (cn function). If any component references `@/lib/utils` instead of `@/utils/style`, do a find-and-replace to fix the import path to match our components.json alias configuration.

    **3. Check that all Radix UI dependencies are in package.json.** The shadcn CLI should install them automatically, but verify these are present:
    - `@radix-ui/react-accordion`
    - `@radix-ui/react-dialog`
    - `@radix-ui/react-dropdown-menu`
    - `@radix-ui/react-label`
    - `@radix-ui/react-popover`
    - `@radix-ui/react-scroll-area`
    - `@radix-ui/react-select`
    - `@radix-ui/react-separator`
    - `@radix-ui/react-slider`
    - `@radix-ui/react-slot`
    - `@radix-ui/react-tabs`
    - `@radix-ui/react-tooltip`
    - `react-resizable-panels`
    - `class-variance-authority`
    - `clsx`
    - `tailwind-merge`

    **4. Verify no component file exceeds 300 lines** (the ESLint max-lines rule from Plan 01 should catch this, but check manually since shadcn components can be large):
    ```bash
    wc -l src/components/ui/*.tsx
    ```
    If any file exceeds 300 lines, split it or trim excess whitespace/comments to fit.
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && ls src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/dialog.tsx src/components/ui/input.tsx src/components/ui/select.tsx src/components/ui/tabs.tsx src/components/ui/accordion.tsx src/components/ui/dropdown-menu.tsx src/components/ui/form.tsx src/components/ui/label.tsx src/components/ui/popover.tsx src/components/ui/scroll-area.tsx src/components/ui/separator.tsx src/components/ui/slider.tsx src/components/ui/textarea.tsx src/components/ui/tooltip.tsx src/components/ui/resizable.tsx 2>&1 && echo "ALL COMPONENTS PRESENT" && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - All 16 shadcn/ui components exist in src/components/ui/
    - Each component imports cn from @/utils/style (not @/lib/utils)
    - All component files are under 300 lines
    - pnpm build succeeds with all components
    - Components use Obsidian Deep CSS variables (var(--background), var(--primary), etc.)
  </done>
  <acceptance_criteria>
    - test -f src/components/ui/button.tsx returns 0
    - test -f src/components/ui/card.tsx returns 0
    - test -f src/components/ui/dialog.tsx returns 0
    - test -f src/components/ui/input.tsx returns 0
    - test -f src/components/ui/select.tsx returns 0
    - test -f src/components/ui/tabs.tsx returns 0
    - test -f src/components/ui/accordion.tsx returns 0
    - test -f src/components/ui/dropdown-menu.tsx returns 0
    - test -f src/components/ui/form.tsx returns 0
    - test -f src/components/ui/label.tsx returns 0
    - test -f src/components/ui/popover.tsx returns 0
    - test -f src/components/ui/scroll-area.tsx returns 0
    - test -f src/components/ui/separator.tsx returns 0
    - test -f src/components/ui/slider.tsx returns 0
    - test -f src/components/ui/textarea.tsx returns 0
    - test -f src/components/ui/tooltip.tsx returns 0
    - test -f src/components/ui/resizable.tsx returns 0
    - grep -r "@/lib/utils" src/components/ui/ returns NO matches (all use @/utils/style)
    - pnpm build exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build infrastructure utilities (AppError, env, logger, storage)</name>
  <files>src/lib/errors.ts, src/lib/env.ts, src/lib/logger.ts, src/lib/storage.ts, src/types/index.ts</files>
  <read_first>
    src/types/index.ts
    .planning/phases/01-foundation-and-design-system/01-CONTEXT.md
  </read_first>
  <action>
    **1. Create `src/lib/errors.ts`** — Custom AppError class per CONTEXT.md:

    ```typescript
    export type ErrorCategory = "ai" | "network" | "storage" | "config" | "validation" | "research" | "unknown";

    export type ErrorCode =
      | "AI_REQUEST_FAILED"
      | "AI_STREAM_ABORTED"
      | "AI_INVALID_RESPONSE"
      | "NETWORK_ERROR"
      | "NETWORK_TIMEOUT"
      | "STORAGE_READ_FAILED"
      | "STORAGE_WRITE_FAILED"
      | "STORAGE_QUOTA_EXCEEDED"
      | "CONFIG_MISSING_KEY"
      | "CONFIG_INVALID_VALUE"
      | "VALIDATION_FAILED"
      | "RESEARCH_STEP_FAILED"
      | "RESEARCH_ABORTED"
      | "UNKNOWN_ERROR";

    interface AppErrorContext {
      [key: string]: unknown;
    }

    export class AppError extends Error {
      readonly code: ErrorCode;
      readonly category: ErrorCategory;
      readonly context: AppErrorContext;
      readonly timestamp: string;

      constructor(
        code: ErrorCode,
        message: string,
        options?: {
          category?: ErrorCategory;
          context?: AppErrorContext;
          cause?: Error;
        }
      ) {
        super(message, { cause: options?.cause });
        this.name = "AppError";
        this.code = code;
        this.category = options?.category ?? this.inferCategory(code);
        this.context = options?.context ?? {};
        this.timestamp = new Date().toISOString();
      }

      private inferCategory(code: ErrorCode): ErrorCategory {
        if (code.startsWith("AI_")) return "ai";
        if (code.startsWith("NETWORK_")) return "network";
        if (code.startsWith("STORAGE_")) return "storage";
        if (code.startsWith("CONFIG_")) return "config";
        if (code.startsWith("VALIDATION_")) return "validation";
        if (code.startsWith("RESEARCH_")) return "research";
        return "unknown";
      }

      toJSON() {
        return {
          isError: true,
          code: this.code,
          category: this.category,
          message: this.message,
          context: this.context,
          timestamp: this.timestamp,
        };
      }
    }

    /** Helper to convert unknown thrown values into AppError */
    export function toAppError(error: unknown, fallbackCode: ErrorCode = "UNKNOWN_ERROR"): AppError {
      if (error instanceof AppError) return error;
      if (error instanceof Error) {
        return new AppError(fallbackCode, error.message, { cause: error });
      }
      return new AppError(fallbackCode, String(error));
    }
    ```

    **2. Create `src/lib/env.ts`** — Centralized env validation with Zod:

    ```typescript
    import { z } from "zod";

    const envSchema = z.object({
      // Server-only env vars (Phase 8 proxy mode)
      ACCESS_PASSWORD: z.string().optional(),
      GOOGLE_GENERATIVE_AI_API_KEY: z.string().optional(),
      GOOGLE_GENERATIVE_AI_API_BASE_URL: z.string().url().optional(),
      OPENAI_API_KEY: z.string().optional(),
      OPENAI_API_BASE_URL: z.string().url().optional(),

      // Search provider keys (Phase 4)
      TAVILY_API_KEY: z.string().optional(),
      TAVILY_API_BASE_URL: z.string().url().optional(),
      FIRECRAWL_API_KEY: z.string().optional(),
      FIRECRAWL_API_BASE_URL: z.string().url().optional(),
      EXA_API_KEY: z.string().optional(),
      EXA_API_BASE_URL: z.string().url().optional(),
      BRAVE_API_KEY: z.string().optional(),
      BRAVE_API_BASE_URL: z.string().url().optional(),
      SEARXNG_API_BASE_URL: z.string().url().optional(),

      // Client-exposed env vars
      NEXT_PUBLIC_DISABLED_AI_PROVIDER: z.string().optional(),
      NEXT_PUBLIC_DISABLED_SEARCH_PROVIDER: z.string().optional(),
      NEXT_PUBLIC_MODEL_LIST: z.string().optional(),
      NEXT_PUBLIC_VERSION: z.string().optional(),
      NEXT_PUBLIC_BUILD_MODE: z.enum(["export", "standalone"]).optional(),
    });

    export type Env = z.infer<typeof envSchema>;

    function loadEnv(): Env {
      const parsed = envSchema.safeParse(process.env);
      if (!parsed.success) {
        console.error("[env] Invalid environment variables:", parsed.error.flatten().fieldErrors);
        throw new Error("Invalid environment variables. Check console for details.");
      }
      return parsed.data;
    }

    /** Validated environment variables. Evaluated once at import time. */
    export const env = loadEnv();
    ```

    **3. Create `src/lib/logger.ts`** — Minimal structured logger:

    ```typescript
    type LogLevel = "debug" | "info" | "warn" | "error";

    const isDev = process.env.NODE_ENV === "development";

    function formatMessage(level: LogLevel, message: string, data?: Record<string, unknown>): string {
      const timestamp = new Date().toISOString();
      if (isDev) {
        const prefix = `[${timestamp}] ${level.toUpperCase()}:`;
        if (data) {
          return `${prefix} ${message} ${JSON.stringify(data, null, 2)}`;
        }
        return `${prefix} ${message}`;
      }
      return JSON.stringify({ level, message, timestamp, ...data });
    }

    export const logger = {
      debug(message: string, data?: Record<string, unknown>) {
        if (isDev) {
          console.debug(formatMessage("debug", message, data));
        }
      },
      info(message: string, data?: Record<string, unknown>) {
        console.info(formatMessage("info", message, data));
      },
      warn(message: string, data?: Record<string, unknown>) {
        console.warn(formatMessage("warn", message, data));
      },
      error(message: string, data?: Record<string, unknown>) {
        console.error(formatMessage("error", message, data));
      },
    };
    ```

    **4. Create `src/lib/storage.ts`** — Zod-validated localforage wrapper:

    ```typescript
    import localforage from "localforage";
    import { z } from "zod";

    // Configure localforage for IndexedDB preference
    localforage.config({
      name: "deep-research",
      storeName: "app_data",
      description: "Deep Research persistent storage",
    });

    /**
     * Type-safe storage get with Zod validation.
     * Returns null if key doesn't exist or validation fails.
     */
    export async function get<T>(
      key: string,
      schema: z.ZodType<T>
    ): Promise<T | null> {
      try {
        const raw = await localforage.getItem(key);
        if (raw === null) return null;
        return schema.parse(raw);
      } catch {
        return null;
      }
    }

    /**
     * Type-safe storage set. Validates value against schema before writing.
     */
    export async function set<T>(
      key: string,
      value: T,
      schema: z.ZodType<T>
    ): Promise<void> {
      const validated = schema.parse(value);
      await localforage.setItem(key, validated);
    }

    /**
     * Remove a key from storage.
     */
    export async function remove(key: string): Promise<void> {
      await localforage.removeItem(key);
    }

    /**
     * Clear all storage data.
     */
    export async function clear(): Promise<void> {
      await localforage.clear();
    }

    export { localforage };
    ```

    **5. Update `src/types/index.ts`** with base type exports:
    ```typescript
    // Base types for Deep Research v1.0
    // Extended during implementation phases

    export interface AppConfig {
      version: string;
      buildMode?: "export" | "standalone";
    }
    ```
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AppError class in src/lib/errors.ts with ErrorCode union, ErrorCategory, toJSON(), and toAppError helper
    - env.ts in src/lib/env.ts with Zod schema validating all env vars, exported `env` object
    - logger.ts in src/lib/logger.ts with debug/info/warn/error methods, JSON in prod, pretty in dev
    - storage.ts in src/lib/storage.ts with Zod-validated get/set/remove/clear using localforage
    - pnpm build succeeds
    - All files under 300 lines
  </done>
  <acceptance_criteria>
    - grep "class AppError" src/lib/errors.ts returns match
    - grep "ErrorCode" src/lib/errors.ts returns match
    - grep "toAppError" src/lib/errors.ts returns match
    - grep "envSchema" src/lib/env.ts returns match
    - grep "export const env" src/lib/env.ts returns match
    - grep "export const logger" src/lib/logger.ts returns match
    - grep "isDev" src/lib/logger.ts returns match
    - grep "export async function get" src/lib/storage.ts returns match
    - grep "export async function set" src/lib/storage.ts returns match
    - grep "localforage" src/lib/storage.ts returns match
    - pnpm build exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. All 16 shadcn/ui components exist and build without errors
2. `grep "class AppError" src/lib/errors.ts` confirms error class
3. `grep "envSchema" src/lib/env.ts` confirms Zod validation
4. `grep "logger" src/lib/logger.ts` confirms logger export
5. `grep "localforage" src/lib/storage.ts` confirms storage wrapper
6. `pnpm build` passes cleanly
7. No file in src/ exceeds 300 lines
</verification>

<success_criteria>
- 16 shadcn/ui components installed with correct import paths (@/utils/style for cn)
- Components render using Obsidian Deep CSS variables (inherited from globals.css)
- AppError provides structured error format: { code, message, category, context, timestamp }
- env.ts validates environment variables with Zod at import time
- logger outputs JSON in production, pretty-print in development
- storage wrapper provides type-safe get/set with Zod validation over localforage
- All files under 300 lines
- Clean build
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-02-SUMMARY.md`
</output>
