---
phase: 01-foundation-and-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/app/design/page.tsx
  - src/app/design/components-demo.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Developer can visit /design and see all 7 surface levels rendered as color swatches"
    - "Developer can see all 4 typography roles (Display, Heading, Label, Body) rendered correctly"
    - "Glassmorphism effect visible on floating elements (backdrop-blur, semi-transparent bg)"
    - "All 16 shadcn/ui components render in Obsidian Deep dark theme on /design"
    - "AI Pulse signature pattern renders as 4px vertical pill in primary color"
    - "No component file exceeds 300 lines"
  artifacts:
    - path: "src/app/design/page.tsx"
      provides: "/design showcase route with all surface, typography, color, pattern, and component demos"
      min_lines: 50
      contains: "Surface Hierarchy"
    - path: "src/app/design/components-demo.tsx"
      provides: "Client component for interactive shadcn/ui component demos"
      contains: "use client"
  key_links:
    - from: "src/app/design/page.tsx"
      to: "src/components/ui/button.tsx"
      via: "imports Button component"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "src/app/design/page.tsx"
      to: "src/app/globals.css"
      via: "uses CSS custom properties and obsidian Tailwind classes"
      pattern: "obsidian-"
---

<objective>
Build the /design showcase route that renders all Obsidian Deep design tokens, signature patterns, and every installed shadcn/ui component in one viewable page, then verify the complete Phase 1 output.

Purpose: The /design route serves as both Phase 1 verification (proving all tokens, fonts, and components render correctly) and as a living reference for all subsequent phases. This is the human verification checkpoint for the entire phase.
Output: A fully rendered /design page demonstrating the complete Obsidian Deep design system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-UI-SPEC.md

<!-- Depends on Plans 01 and 02 output -->
@.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-design-system/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 — Tailwind obsidian tokens available -->
Tailwind classes: bg-obsidian-surface-well, bg-obsidian-surface-deck, bg-obsidian-surface-sheet, bg-obsidian-surface-raised, bg-obsidian-surface-float, bg-obsidian-surface-bright, text-obsidian-primary, text-obsidian-on-surface, text-obsidian-on-surface-var, bg-obsidian-primary, bg-obsidian-primary-deep, text-obsidian-secondary, text-obsidian-tertiary, bg-obsidian-error, bg-obsidian-error-bg

<!-- From Plan 02 — All shadcn components in src/components/ui/ -->
Available imports: Button, Card, Dialog, Input, Select, Tabs, Accordion, DropdownMenu, Form, Label, Popover, ScrollArea, Separator, Slider, Textarea, Tooltip, ResizablePanelGroup/Panel/Handle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /design showcase route</name>
  <files>src/app/design/page.tsx, src/app/design/components-demo.tsx</files>
  <read_first>
    src/app/globals.css
    tailwind.config.ts
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/input.tsx
    .planning/phases/01-foundation-and-design-system/01-UI-SPEC.md
    design/DESIGN.md
  </read_first>
  <action>
    Create `src/app/design/page.tsx` as a Server Component (no "use client" needed for static showcase).

    The page must have 5 sections, each clearly labeled:

    **Section 1: Surface Hierarchy Swatches**
    Render 7 color swatches in a horizontal row, each labeled with role name and hex value:
    - Well (#0e0e10) — `bg-obsidian-surface-well`
    - Surface (#131315) — `bg-obsidian-surface`
    - Deck (#1c1b1d) — `bg-obsidian-surface-deck`
    - Sheet (#201f22) — `bg-obsidian-surface-sheet`
    - Raised (#2a2a2c) — `bg-obsidian-surface-raised`
    - Float (#353437) — `bg-obsidian-surface-float`
    - Bright (#39393b) — `bg-obsidian-surface-bright`

    Each swatch: 80x80px rounded rectangle with the role name in small text below. Background of this section is `bg-obsidian-surface-well` so the Well swatch is invisible (proving correct hierarchy).

    **Section 2: Typography Scale**
    Render all 4 declared roles plus micro text:
    - Display: 36px, semibold, tight tracking — "Display Heading"
    - Heading: 20px, semibold, tight tracking — "Section Heading"
    - Label: 12px, semibold, wider tracking — "LABEL TEXT"
    - Body: 14px, regular, relaxed leading — "Body text with relaxed line height for readability in research content."
    - Micro (in JetBrains Mono): 10px, medium — "2024-01-15T12:30:00Z" (timestamp example)
    Use the exact Tailwind classes from UI-SPEC: `text-4xl font-semibold tracking-tight`, `text-xl font-semibold tracking-tight`, `text-xs font-semibold tracking-wider`, `text-sm font-normal leading-relaxed`, `text-[10px] font-mono font-medium`.

    **Section 3: Color Swatches**
    Render labeled color pills for:
    - Primary (#c0c1ff), Primary Deep (#8083ff), Primary Seed (#6366f1)
    - Secondary (#adc6ff), Tertiary (#d0bcff)
    - On Surface (#e5e1e4), On Surface Var (#c7c4d7)
    - Error (#ffb4ab), Error BG (#93000a)
    Each with a small background swatch and text showing the hex value.

    **Section 4: Signature Patterns**
    Render 4 pattern demos:
    1. Glassmorphism: A div with `bg-[rgba(32,31,34,0.6)] backdrop-blur-[20px] rounded-xl shadow-[0px_24px_48px_-12px_rgba(0,0,0,0.5)]` containing "Glassmorphism Panel" text
    2. Primary Gradient Button: A div with `bg-gradient-to-br from-obsidian-primary to-obsidian-primary-deep text-[#1000a9] rounded-xl px-6 py-2` containing "Start Research"
    3. AI Pulse: A flex row with a 4px-wide, full-height div (`w-1 rounded-sm bg-obsidian-primary`) next to text "AI-generated content indicator"
    4. Focus Ring: An input with `shadow-[0_0_0_2px_rgba(192,193,255,0.2)]` showing the focus ring style

    **Section 5: Component Library**
    Import and render each of the 16 shadcn/ui components with minimal interactive state. Use "use client" directive only if needed (wrap interactive components in a small client boundary).

    Since this is primarily a Server Component page, create a small client wrapper `src/app/design/components-demo.tsx` (under 300 lines) for interactive components that need state (Dialog open/close, Tabs selection, Slider value, etc.). The main page.tsx imports and renders this client component.

    Show at minimum:
    - Button: default, secondary, destructive, outline, ghost variants
    - Card: a sample card with header and content
    - Input: a text input with label
    - Select: a basic select dropdown
    - Tabs: 2-3 sample tabs
    - Dialog: open/close dialog
    - Textarea: a multi-line input
    - Accordion: 2-3 collapsible items
    - DropdownMenu: a dropdown menu
    - Slider: a value slider
    - Tooltip: hover tooltip
    - ScrollArea: a scrollable area
    - Separator: horizontal separator
    - Label: form label
    - Popover: a popover
    - Resizable: resizable panel layout

    Keep the page under 300 lines by extracting the client interactive demo into a separate file. The Server Component page.tsx handles sections 1-4 (static). The client component handles section 5 (interactive).

    **IMPORTANT:** Create the file `src/app/design/components-demo.tsx` with `"use client"` directive for the interactive section. This keeps page.tsx as a Server Component for sections 1-4.

    Structure:
    - `src/app/design/page.tsx` — Server Component, sections 1-4 (static tokens) + imports ComponentsDemo
    - `src/app/design/components-demo.tsx` — Client Component ("use client"), section 5 (interactive components)
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && pnpm build 2>&1 | tail -5 && test -f src/app/design/page.tsx && echo "design page exists" && test -f src/app/design/components-demo.tsx && echo "components demo exists"</automated>
  </verify>
  <done>
    - /design route builds and renders without errors
    - Surface hierarchy shows all 7 levels with correct colors and labels
    - Typography shows all 4 roles + micro text with correct fonts
    - Signature patterns render: glassmorphism, gradient button, AI Pulse, focus ring
    - All 16 shadcn/ui components render in dark theme
    - page.tsx under 300 lines, components-demo.tsx under 300 lines
  </done>
  <acceptance_criteria>
    - test -f src/app/design/page.tsx returns 0
    - test -f src/app/design/components-demo.tsx returns 0
    - grep "Surface Hierarchy" src/app/design/page.tsx returns match
    - grep "Typography" src/app/design/page.tsx returns match
    - grep "obsidian-surface-well" src/app/design/page.tsx returns match
    - grep "AI Pulse" src/app/design/page.tsx returns match
    - grep "Glassmorphism" src/app/design/page.tsx returns match
    - grep "use client" src/app/design/components-demo.tsx returns match
    - grep "Button" src/app/design/components-demo.tsx returns match
    - wc -l src/app/design/page.tsx shows <= 300
    - wc -l src/app/design/components-demo.tsx shows <= 300
    - pnpm build exits with code 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify /design showcase renders correctly</name>
  <files>src/app/design/page.tsx</files>
  <read_first>
    src/app/design/page.tsx
    src/app/design/components-demo.tsx
  </read_first>
  <action>
    Human verification of the complete Phase 1 Foundation and Design System output.

    Complete Phase 1 output to verify:
    - Obsidian Deep CSS custom properties and Tailwind configuration
    - Inter + JetBrains Mono fonts via next/font
    - 16 shadcn/ui components installed and themed
    - Infrastructure utilities (AppError, env, logger, storage)
    - /design showcase route rendering all tokens and components
    - ESLint 300-line max rule
    - Unused packages removed

    Verification steps:
    1. Run `pnpm dev` to start the dev server
    2. Visit http://localhost:3000 — should show dark Obsidian background (#0e0e10) with Inter font, text "Deep Research" in #e5e1e4
    3. Visit http://localhost:3000/design — should show the complete showcase:
       a. Surface Hierarchy: 7 color swatches from Well (#0e0e10) to Bright (#39393b) — tonal layering visible, no borders
       b. Typography: Display at 36px, Heading at 20px, Label at 12px uppercase, Body at 14px, Micro timestamp in JetBrains Mono
       c. Signature Patterns: Glass panel with blur, gradient "Start Research" button, 4px AI Pulse pill, focus ring on input
       d. Components: All shadcn/ui components rendering in dark theme — buttons, cards, dialogs, inputs, tabs, etc.
    4. Check: No pure white (#fff) text anywhere — all text should be #e5e1e4 or #c7c4d7
    5. Check: No visible 1px borders between surface sections — only tonal shifts
    6. Check: Glassmorphism panel has visible blur effect and semi-transparent background
    7. Run `pnpm lint` to verify ESLint passes
    8. Run `pnpm build` to verify production build succeeds
  </action>
  <verify>
    <automated>cd /home/converter/code/deep-research && pnpm lint 2>&1 | tail -3 && pnpm build 2>&1 | tail -3 && echo "BUILD AND LINT PASSED"</automated>
  </verify>
  <done>
    - Human confirms /design page renders all surface hierarchy levels with correct colors
    - Human confirms typography roles display with correct sizes, weights, and fonts
    - Human confirms glassmorphism, gradient button, AI Pulse, and focus ring patterns render
    - Human confirms all shadcn/ui components render in dark Obsidian Deep theme
    - Human confirms no pure white text and no 1px solid borders
    - pnpm lint passes
    - pnpm build succeeds
  </done>
  <acceptance_criteria>
    - grep "Surface Hierarchy" src/app/design/page.tsx returns match
    - pnpm build exits with code 0
    - pnpm lint exits with code 0
    - Human types "approved" confirming visual correctness
  </acceptance_criteria>
  <what-built>
    Complete Phase 1 Foundation and Design System:
    - Obsidian Deep CSS custom properties and Tailwind configuration
    - Inter + JetBrains Mono fonts via next/font
    - 16 shadcn/ui components installed and themed
    - Infrastructure utilities (AppError, env, logger, storage)
    - /design showcase route rendering all tokens and components
    - ESLint 300-line max rule
    - Unused packages removed
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` to start the dev server
    2. Visit http://localhost:3000 — should show dark Obsidian background (#0e0e10) with Inter font, text "Deep Research" in #e5e1e4
    3. Visit http://localhost:3000/design — should show the complete showcase:
       a. Surface Hierarchy: 7 color swatches from Well (#0e0e10) to Bright (#39393b) — tonal layering visible, no borders
       b. Typography: Display at 36px, Heading at 20px, Label at 12px uppercase, Body at 14px, Micro timestamp in JetBrains Mono
       c. Signature Patterns: Glass panel with blur, gradient "Start Research" button, 4px AI Pulse pill, focus ring on input
       d. Components: All shadcn/ui components rendering in dark theme — buttons, cards, dialogs, inputs, tabs, etc.
    4. Check: No pure white (#fff) text anywhere — all text should be #e5e1e4 or #c7c4d7
    5. Check: No visible 1px borders between surface sections — only tonal shifts
    6. Check: Glassmorphism panel has visible blur effect and semi-transparent background
    7. Run `pnpm lint` to verify ESLint passes
    8. Run `pnpm build` to verify production build succeeds
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm dev` starts and /design renders all 5 sections
2. Surface colors match hex values in UI-SPEC
3. Inter font loads for body, JetBrains Mono for code
4. All shadcn components render in dark theme
5. Glassmorphism effect visible (backdrop-blur)
6. No pure white text (#fff)
7. No 1px solid borders
8. `pnpm lint` passes
9. `pnpm build` succeeds
10. All files under 300 lines
</verification>

<success_criteria>
- /design page renders all 7 surface hierarchy levels as labeled color swatches
- All 4 typography roles + micro text render with correct size, weight, tracking
- Glassmorphism, gradient button, AI Pulse, and focus ring patterns all render
- All 16 shadcn/ui components render with Obsidian Deep dark styling
- No file in the project exceeds 300 lines (ESLint enforced)
- pnpm build and pnpm lint pass cleanly
- Human confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-03-SUMMARY.md`
</output>
